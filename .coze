[project]
requires = ["python3-3.12"]

[dev]
build = ["sh", "-c", "pip install -r server/requirements.txt && pnpm install"]
run = ["sh", "server/dev.sh"]

[deploy]
build = ["pip", "install", "-r", "server/requirements.txt"]
run = ["sh", "server/start.sh"]