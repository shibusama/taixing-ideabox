[project]
requires = ["python3-3.12"]

[dev]
build = ["pnpm", "install"]
run = ["pnpm", "run", "dev"]

[deploy]
build = ["bash", "-c", "pip install -r server/requirements.txt && pnpm install && pnpm run build"]
run = ["sh", "server/start.sh"]