[project]
requires = ["nodejs-24"]

[dev]
build = ["pnpm", "install"]
run = ["pnpm", "run", "dev"]

[deploy]
build = ["pnpm", "install"]
run = ["sh", "-c", "pnpm run build && npx serve dist -l ${DEPLOY_RUN_PORT}"]
